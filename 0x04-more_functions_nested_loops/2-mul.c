#include "main.h"
#include <stdio.h>

/**
 * mul -> multiplies two integers
 * @a: int to multiplied
 * @b: int to multiplied
 * Return: product of two int's
 */

int mul(int a, int b)
{
	return (a * b);
}
